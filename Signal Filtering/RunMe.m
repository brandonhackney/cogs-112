% This runs Filtering.mlapp, which opens a GUI
% It lets you apply different filters to a signal and watch the outcome
% (if you just double-click the file, it edits it instead of running)

Filtering;
% (You also have the option of controlling the signal and filter freqs)